# French/English Language Switch Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Let players switch Pack Ema's UI between French and English via a small
switcher in the header, using Django's standard i18n machinery.

**Architecture:** `django.middleware.locale.LocaleMiddleware` + a `LANGUAGES` setting
with `en`/`fr`; no URL prefixing. A POST form in `base.html` hits Django's built-in
`set_language` view to store the choice in a session cookie. Every UI string in
templates and Python-side messages is wrapped in `{% trans %}`/`{% blocktrans %}` or
`gettext_lazy`; a hand-authored `game/locale/fr/LC_MESSAGES/django.po` supplies the
French text, compiled to `.mo` with Django's `compilemessages`.

**Tech Stack:** Django 6.1's built-in `django.utils.translation` / `django.middleware.locale`
/ `django.views.i18n` — no new dependencies. GNU `gettext` (the `msgfmt`/`xgettext`
binaries, not a Python package) must be installed in the Docker image for
`makemessages`/`compilemessages` to run.

**Spec:** `docs/superpowers/specs/2026-09-24-i18n-language-switch-design.md`

## Global Constraints

- No URL-prefixed locales (`i18n_patterns` is **not** used) — every existing route
  keeps its current path.
- `LANGUAGE_CODE` stays `'en-us'` (the fallback when the browser doesn't ask for
  French and the user hasn't picked a language yet).
- `LANGUAGES = [('en', 'English'), ('fr', 'Français')]`.
- Card content scraped from Komi (names, taglines, descriptions) is **never**
  translated — only the app's own UI chrome, messages, and the `Rarity`/`Status`
  choice labels.
- The existing test suite (`game.tests.*`) must keep passing unchanged: it never
  sets `Accept-Language`, so it always exercises the English (default) strings.
  Every `{% trans %}`/`gettext_lazy` wrap must reproduce byte-identical English
  output for msgids that existing tests assert on verbatim (see each task's
  "Files already asserting this text" notes).
- All commands in this plan run through Docker Compose per `CLAUDE.md`
  (`docker compose exec web python manage.py ...`). **Docker Desktop must be
  running** before executing any step — if `docker compose ps` fails with a
  daemon-connection error, start Docker Desktop first.

---

### Task 1: I18n routing & infrastructure

**Files:**
- Modify: `tcg/settings.py` (`MIDDLEWARE`, add `LANGUAGES`, add `LOCALE_PATHS`)
- Modify: `tcg/urls.py` (wire Django's built-in `set_language` view)
- Modify: `Dockerfile` (install the `gettext` system package)
- Create: `game/tests/test_i18n.py`

**Interfaces:**
- Produces: URL name `set_language` (POST, params `language`, `next`) — consumed
  by Task 2's switcher form.
- Produces: `settings.LANGUAGES` (list of `(code, label)` tuples) — consumed by
  Task 2 for the switcher and implicitly by every later `{% trans %}` tag.

- [ ] **Step 1: Write the failing test**

Create `game/tests/test_i18n.py`:

```python
from django.conf import settings
from django.test import TestCase
from django.urls import reverse


class I18nInfrastructureTests(TestCase):
    def test_locale_middleware_is_installed_between_session_and_common(self):
        session_index = settings.MIDDLEWARE.index("django.contrib.sessions.middleware.SessionMiddleware")
        locale_index = settings.MIDDLEWARE.index("django.middleware.locale.LocaleMiddleware")
        common_index = settings.MIDDLEWARE.index("django.middleware.common.CommonMiddleware")
        self.assertTrue(session_index < locale_index < common_index)

    def test_french_is_an_available_language(self):
        self.assertIn(("fr", "Français"), settings.LANGUAGES)

    def test_set_language_view_sets_cookie_and_redirects(self):
        response = self.client.post(reverse("set_language"), {"language": "fr", "next": "/"})
        self.assertRedirects(response, "/", fetch_redirect_response=False)
        self.assertEqual(self.client.cookies[settings.LANGUAGE_COOKIE_NAME].value, "fr")
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `docker compose exec web python manage.py test game.tests.test_i18n -v 2`
Expected: FAIL — `AttributeError`/`ValueError` on `settings.LANGUAGES` not matching
(default Django `LANGUAGES` is the huge built-in list, so the `assertIn` fails) and
`NoReverseMatch: Reverse for 'set_language' not found`.

- [ ] **Step 3: Wire the middleware, settings and URL**

In `tcg/settings.py`, insert `LocaleMiddleware` right after `SessionMiddleware`
(Django requires it before `CommonMiddleware`, which uses the active language for
things like the `Content-Language` header):

```python
MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.locale.LocaleMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]
```

Replace the internationalization section:

```python
# Internationalization
# https://docs.djangoproject.com/en/6.1/topics/i18n/

LANGUAGE_CODE = 'en-us'

LANGUAGES = [
    ('en', 'English'),
    ('fr', 'Français'),
]

LOCALE_PATHS = [BASE_DIR / "game" / "locale"]

TIME_ZONE = 'UTC'

USE_I18N = True

USE_TZ = True
```

In `tcg/urls.py`, add the include for Django's i18n URLs (this registers
`set_language` at `/i18n/setlang/`):

```python
from django.conf import settings
from django.conf.urls.static import static
from django.contrib import admin
from django.contrib.auth import views as auth_views
from django.urls import include, path

from game import views

urlpatterns = [
    path("i18n/", include("django.conf.urls.i18n")),
    path("", views.home, name="home"),
    path("packs/open/", views.open_pack_view, name="open_pack"),
    path("collection/", views.collection, name="collection"),
    path("cards/<int:pk>/", views.card_detail, name="card_detail"),
    path("cards/propose/", views.propose_card, name="propose_card"),
    path("friends/", views.friends, name="friends"),
    path("friends/<int:pk>/accept/", views.friend_accept, name="friend_accept"),
    path("friends/<int:pk>/remove/", views.friend_remove, name="friend_remove"),
    path("friends/<str:username>/collection/", views.friend_collection, name="friend_collection"),
    path("signup/", views.signup, name="signup"),
    path("login/", auth_views.LoginView.as_view(), name="login"),
    path("logout/", auth_views.LogoutView.as_view(), name="logout"),
    path("admin/", admin.site.urls),
] + static(settings.MEDIA_URL, document_root=settings.MEDIA_ROOT)  # only active when DEBUG
```

In `Dockerfile`, install `gettext` (needed for `makemessages`/`compilemessages` in
Task 8) right after the `uv` binary is copied in, before app files change often:

```dockerfile
FROM python:3.12-slim
COPY --from=ghcr.io/astral-sh/uv:latest /uv /bin/uv
RUN apt-get update && apt-get install -y --no-install-recommends gettext \
    && rm -rf /var/lib/apt/lists/*
# venv lives outside /app so the bind mount in compose.yaml doesn't hide it
ENV UV_PROJECT_ENVIRONMENT=/opt/venv PATH="/opt/venv/bin:$PATH" PYTHONUNBUFFERED=1
WORKDIR /app
COPY pyproject.toml uv.lock ./
RUN uv sync --frozen --no-install-project
COPY . .
EXPOSE 8000
CMD ["sh", "-c", "python manage.py migrate --noinput && python manage.py runserver 0.0.0.0:8000"]
```

- [ ] **Step 4: Rebuild the image and run the test to verify it passes**

Run: `docker compose build web && docker compose exec web python manage.py test game.tests.test_i18n -v 2`

(If `web` isn't already running, use `docker compose up -d db && docker compose run --rm web python manage.py test game.tests.test_i18n -v 2` instead.)

Expected: PASS (3 tests).

- [ ] **Step 5: Run the full suite to confirm no regression**

Run: `docker compose exec web python manage.py test game`
Expected: PASS, same test count as before this task.

- [ ] **Step 6: Commit**

```bash
git add tcg/settings.py tcg/urls.py Dockerfile game/tests/test_i18n.py
git commit -m "feat: wire Django i18n middleware, LANGUAGES and set_language URL"
```

---

### Task 2: Language switcher UI in the header

**Files:**
- Modify: `game/templates/base.html`
- Modify: `game/static/game/tcg.css`
- Modify: `game/tests/test_i18n.py`

**Interfaces:**
- Consumes: URL name `set_language` (Task 1).
- Produces: `.topbar-right` / `.lang-switch` markup other templates don't need to
  know about (self-contained in `base.html`).

**Files already asserting text this task touches:** none check literal nav text
(only `href` attributes), so wrapping in `{% trans %}` is a pure regression check
here — see `game/tests/test_friends.py:118`.

- [ ] **Step 1: Write the failing test**

Add to `game/tests/test_i18n.py` (new imports at top: `from game.models import User`):

```python
from django.conf import settings
from django.test import TestCase
from django.urls import reverse

from game.models import User


class I18nInfrastructureTests(TestCase):
    # ... (unchanged from Task 1)


class LanguageSwitcherTests(TestCase):
    def setUp(self):
        self.user = User.objects.create_user("ash", password="pw")
        self.client.force_login(self.user)

    def test_switcher_posts_to_set_language_with_both_options(self):
        response = self.client.get(reverse("home"))
        self.assertContains(response, f'action="{reverse("set_language")}"')
        self.assertContains(response, 'name="language" value="fr"')
        self.assertContains(response, 'name="language" value="en"')

    def test_english_is_marked_active_by_default(self):
        response = self.client.get(reverse("home"))
        self.assertContains(response, 'value="en" class="active"')

    def test_switcher_is_visible_when_logged_out(self):
        self.client.logout()
        response = self.client.get(reverse("login"))
        self.assertContains(response, f'action="{reverse("set_language")}"')
```

- [ ] **Step 2: Run the tests to verify they fail**

Run: `docker compose exec web python manage.py test game.tests.test_i18n.LanguageSwitcherTests -v 2`
Expected: FAIL — no `.lang-switch` form exists yet in `base.html`.

- [ ] **Step 3: Rewrite `base.html`**

```html
{% load static i18n %}{% get_current_language as CURRENT_LANGUAGE %}<!doctype html>
<html lang="{{ CURRENT_LANGUAGE }}">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{% block title %}Pack Ema{% endblock %}</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;600;800;900&display=swap">
  <link rel="stylesheet" href="{% static 'game/tcg.css' %}">
  {% block head %}{% endblock %}
</head>
<body>
  {% with here=request.resolver_match.url_name %}
  <header class="topbar">
    <a class="brand" href="{% url 'home' %}"><span class="brand-mark">EMA</span> Pack Ema</a>
    <div class="topbar-right">
      {% if user.is_authenticated %}
      <nav class="nav">
        <a href="{% url 'home' %}" {% if here == 'home' %}class="active" aria-current="page"{% endif %}>{% trans "Packs" %}</a>
        <a href="{% url 'collection' %}" {% if here == 'collection' or here == 'card_detail' %}class="active" aria-current="page"{% endif %}>{% trans "Collection" %}</a>
        <a href="{% url 'friends' %}" {% if here == 'friends' or here == 'friend_collection' %}class="active" aria-current="page"{% endif %}>{% trans "Friends" %}</a>
        <a href="{% url 'propose_card' %}" {% if here == 'propose_card' %}class="active" aria-current="page"{% endif %}>{% trans "Propose" %}</a>
        <form method="post" action="{% url 'logout' %}">{% csrf_token %}<button class="nav-logout" title="{% blocktrans with username=user.username %}Log out {{ username }}{% endblocktrans %}">{% trans "Log out" %}</button></form>
      </nav>
      {% endif %}
      <form class="lang-switch" method="post" action="{% url 'set_language' %}">
        {% csrf_token %}
        <input type="hidden" name="next" value="{{ request.path }}">
        <button type="submit" name="language" value="en" class="{% if CURRENT_LANGUAGE == 'en' %}active{% endif %}" aria-label="{% trans 'Switch to English' %}">EN</button>
        <button type="submit" name="language" value="fr" class="{% if CURRENT_LANGUAGE == 'fr' %}active{% endif %}" aria-label="{% trans 'Switch to French' %}">FR</button>
      </form>
    </div>
  </header>
  {% endwith %}
  <main>
    {% for message in messages %}<p class="flash flash-{{ message.tags }}" role="status">{{ message }}</p>{% endfor %}
    {% block content %}{% endblock %}
  </main>
  {% block scripts %}{% endblock %}
</body>
</html>
```

- [ ] **Step 4: Add the switcher CSS**

Append to `game/static/game/tcg.css` (after the `.nav` rules, around line 63):

```css
.topbar-right { display: flex; align-items: center; gap: 8px; }
.lang-switch { display: flex; gap: 2px; margin: 0; }
.lang-switch button {
  padding: 6px 10px; border-radius: 999px; border: 0; background: none; color: var(--muted);
  font: 700 .78rem Outfit, system-ui, sans-serif; letter-spacing: .04em; cursor: pointer; transition: color .15s, background .15s;
}
.lang-switch button:hover { color: var(--text); background: var(--surface-2); }
.lang-switch button.active { color: var(--text); background: var(--surface-2); box-shadow: inset 0 0 0 1px var(--line-strong); }
@media (max-width: 480px) { .lang-switch button { padding: 6px 8px; } }
```

- [ ] **Step 5: Run the tests to verify they pass**

Run: `docker compose exec web python manage.py test game.tests.test_i18n -v 2`
Expected: PASS (6 tests total: 3 from Task 1 + 3 new).

- [ ] **Step 6: Run the full suite to confirm no regression**

Run: `docker compose exec web python manage.py test game`
Expected: PASS.

- [ ] **Step 7: Commit**

```bash
git add game/templates/base.html game/static/game/tcg.css game/tests/test_i18n.py
git commit -m "feat: add FR/EN language switcher to the header"
```

---

### Task 3: Rarity & status labels translated

**Files:**
- Modify: `game/models.py`

**Interfaces:**
- Produces: `Rarity.choices` / `Status.choices` with translatable labels — consumed
  everywhere `get_rarity_display()`/`get_status_display()` is already called
  (`game/templates/game/_card.html`, `card_detail.html`, `collection.html`,
  `propose.html`), with no template changes needed for this specifically.

**Files already asserting this text:** `game/tests/test_models.py:16` —
`self.assertEqual(card.get_rarity_display(), "Legendary")`. `gettext_lazy` proxies
compare equal to plain strings, and the test runs in English (no `Accept-Language`
set), so this must keep passing unchanged.

- [ ] **Step 1: Make the change**

In `game/models.py`, add the import and wrap both choice classes' labels:

```python
from django.contrib.auth.models import AbstractUser
from django.db import models
from django.utils.translation import gettext_lazy as _


class User(AbstractUser):
    # None = never opened a pack, so one is available right away.
    next_pack_at = models.DateTimeField(null=True, blank=True)


class Rarity(models.IntegerChoices):
    COMMON = 1, _("Common")
    UNCOMMON = 2, _("Uncommon")
    RARE = 3, _("Rare")
    EPIC = 4, _("Epic")
    LEGENDARY = 5, _("Legendary")


class Status(models.TextChoices):
    PENDING = "pending", _("Pending review")
    APPROVED = "approved", _("Approved")
    REJECTED = "rejected", _("Rejected")
```

(Leave `Card`, `CardProposal`, `Pull`, and `Friendship` — including its own nested
`Friendship.Status` — untouched; `Friendship.Status`'s "Pending"/"Accepted" labels
are never rendered via `get_status_display()` anywhere, so there's nothing to wrap.)

- [ ] **Step 2: Run the full suite to verify nothing broke**

Run: `docker compose exec web python manage.py test game`
Expected: PASS — in particular `test_models.py::RarityTests` (or equivalent) and
every `test_proposals.py` test that touches `get_rarity_display()`/`get_status_display()`.

- [ ] **Step 3: Commit**

```bash
git add game/models.py
git commit -m "feat: make Rarity and Status choice labels translatable"
```

---

### Task 4: Card & collection templates translated

**Files:**
- Modify: `game/templates/game/home.html` (static text only — data attributes for
  `pack.js` come in Task 7)
- Modify: `game/templates/game/_card.html`
- Modify: `game/templates/game/collection.html`
- Modify: `game/templates/game/card_detail.html`

**Files already asserting text this task touches:**
- `game/tests/test_views.py:36-37` — `data-open-url="..."`, `data-next-pack-at=""` (untouched attributes, must survive).
- `game/tests/test_views.py:87` and `game/tests/test_proposals.py:170` —
  `"1 of 2 cards discovered"` / `"1 of 1 cards discovered"` — the `blocktrans` for
  the progress aria-label must render byte-identical English output.
- `game/tests/test_views.py:91-93` — `id="pack"`, `game/pack.js`, `game/fx.js` (untouched).
- `game/tests/test_views.py:100-101` — `"Bienvenue au BDE"` (card content, untouched), `"<dd>×2</dd>"`.
- `game/tests/test_proposals.py:55` — `class="card rarity-common"` (untouched).

- [ ] **Step 1: Rewrite `game/templates/game/home.html`**

```html
{% extends "base.html" %}{% load static i18n %}
{% block head %}<link rel="stylesheet" href="{% static 'game/pack.css' %}">{% endblock %}
{% block content %}
<section id="stage" class="stage" data-open-url="{% url 'open_pack' %}" data-next-pack-at="{{ next_pack_at|default:'' }}">
  {% csrf_token %}
  <div id="packWrap" class="pack-wrap">
    <div id="pack" class="pack" tabindex="0" role="button" aria-label="{% trans 'Open a pack: drag across the top edge, or press Enter' %}">
      <div class="pack-foil">
        <div class="pack-body">
          <div class="pack-logo">EMA</div>
          <div class="pack-sub">{% trans "Trading Card Game · IMT Alès" %}</div>
        </div>
        <div class="pack-shine"></div>
        <div class="pack-glare"></div>
      </div>
      <div class="pack-top"><div id="tearLine" class="tear-line"></div></div>
    </div>
    <p id="hint" class="hint">{% trans "Swipe across the top to tear it open" %}</p>
    <p id="timer" class="timer" role="timer"></p>
  </div>

  <div id="summary" class="summary" hidden>
    <h2>{% trans "Your pulls" %}</h2>
    <div id="summaryCards" class="summary-cards"></div>
    <div class="summary-actions">
      <a class="btn" href="{% url 'collection' %}">{% trans "View collection" %}</a>
      <button id="again" class="btn ghost" type="button">{% trans "Back" %}</button>
    </div>
  </div>
</section>
<div id="reveal" class="reveal" hidden></div>
<button id="skip" class="btn ghost skip" type="button" hidden>{% trans "Skip" %} ⏭</button>
<canvas id="fx" class="fx" aria-hidden="true"></canvas>
{% endblock %}
{% block scripts %}
<script src="{% static 'game/fx.js' %}"></script>
<script src="{% static 'game/pack.js' %}"></script>
{% endblock %}
```

- [ ] **Step 2: Rewrite `game/templates/game/_card.html`**

```html
{% load i18n %}<article class="card rarity-{{ card.rarity_slug }}" style="--c1: {{ card.primary_color|default:'#3a3f58' }}; --c2: {{ card.secondary_color|default:'#1b1e2e' }}">
  <div class="card-inner">
    <div class="card-art">{% if card.banner %}<img src="{{ card.banner.url }}" alt="" loading="lazy">{% endif %}</div>
    <div class="card-logo">{% if card.logo %}<img src="{{ card.logo.url }}" alt="" loading="lazy">{% else %}<span>{{ card.name|slice:":1"|upper }}</span>{% endif %}</div>
    <h3 class="card-name">{{ card.name }}</h3>
    <p class="card-tagline">{{ card.tagline|truncatechars:90 }}</p>
    <dl class="card-stats">
      <div><dt>{% trans "Subs" %}</dt><dd>{{ card.subscriber_count }}</dd></div>
      <div><dt>{% trans "Posts" %}</dt><dd>{{ card.post_count }}</dd></div>
      <div><dt>{% trans "Events" %}</dt><dd>{{ card.event_count }}</dd></div>
    </dl>
    <span class="card-rarity">{{ card.get_rarity_display }}</span>
    <div class="card-shine"></div>
    <div class="card-glare"></div>
  </div>
</article>
```

- [ ] **Step 3: Rewrite `game/templates/game/collection.html`**

```html
{% extends "base.html" %}{% load static i18n %}
{% block title %}{% if owner %}{% blocktrans with username=owner.username %}{{ username }}'s collection{% endblocktrans %}{% else %}{% trans "Collection" %}{% endif %} · Pack Ema{% endblock %}
{% block content %}
{% with owned=cards|length %}
<header class="page-head">
  <h1>{% if owner %}{% blocktrans with username=owner.username %}{{ username }}'s collection{% endblocktrans %}{% else %}{% trans "Collection" %}{% endif %}</h1>
  <div class="progress" role="img" aria-label="{% blocktrans %}{{ owned }} of {{ total }} cards discovered{% endblocktrans %}">
    <div class="progress-bar"><span style="width: {% widthratio owned total 100 %}%"></span></div>
    <span class="progress-label"><strong>{{ owned }}</strong> {% blocktrans %}/ {{ total }} discovered{% endblocktrans %}</span>
  </div>
</header>
{% if not owned %}
<div class="panel empty">
  <span class="brand-mark big">EMA</span>
  {% if owner %}
  <h2>{% trans "No cards yet" %}</h2>
  <p class="muted">{% blocktrans with username=owner.username %}{{ username }} hasn't discovered any cards yet.{% endblocktrans %}</p>
  {% else %}
  <h2>{% trans "No cards yet" %}</h2>
  <p class="muted">{% trans "Tear open your first pack to start collecting." %}</p>
  <a class="btn" href="{% url 'home' %}">{% trans "Open a pack" %}</a>
  {% endif %}
</div>
{% endif %}
{% endwith %}
<div class="grid">
  {% for card in cards %}
  {% if owner %}
  <div class="grid-item" aria-label="{{ card.name }}, {{ card.get_rarity_display }}">
    {% include "game/_card.html" %}
    {% if card.copies > 1 %}<span class="copies">×{{ card.copies }}</span>{% endif %}
  </div>
  {% else %}
  <a class="grid-item" href="{% url 'card_detail' card.pk %}" aria-label="{{ card.name }}, {{ card.get_rarity_display }}">
    {% include "game/_card.html" %}
    {% if card.copies > 1 %}<span class="copies">×{{ card.copies }}</span>{% endif %}
  </a>
  {% endif %}
  {% endfor %}
</div>
{% endblock %}
{% block scripts %}
<script src="{% static 'game/fx.js' %}"></script>
<script>document.querySelectorAll('.card').forEach(el => FX.tilt(el));</script>
{% endblock %}
```

- [ ] **Step 4: Rewrite `game/templates/game/card_detail.html`**

```html
{% extends "base.html" %}{% load static i18n %}
{% block title %}{{ card.name }} · Pack Ema{% endblock %}
{% block content %}
<a class="back" href="{% url 'collection' %}">← {% trans "Collection" %}</a>
<div class="detail">
  {% include "game/_card.html" %}
  <section class="panel detail-text rarity-{{ card.rarity_slug }}">
    <span class="chip">{{ card.get_rarity_display }}</span>
    <h1>{{ card.name }}</h1>
    {% if card.tagline %}<p class="muted">{{ card.tagline }}</p>{% endif %}
    <dl class="stats">
      <div><dt>{% trans "Owned" %}</dt><dd>×{{ copies }}</dd></div>
      <div><dt>{% trans "Subscribers" %}</dt><dd>{{ card.subscriber_count }}</dd></div>
      <div><dt>{% trans "Posts" %}</dt><dd>{{ card.post_count }}</dd></div>
      <div><dt>{% trans "Events" %}</dt><dd>{{ card.event_count }}</dd></div>
    </dl>
    <div class="prose">
      {% if card.description %}{{ card.description|linebreaks }}{% else %}<p class="muted">{% trans "This page has no presentation yet." %}</p>{% endif %}
    </div>
  </section>
</div>
{% endblock %}
{% block scripts %}
<script src="{% static 'game/fx.js' %}"></script>
<script>document.querySelectorAll('.card').forEach(el => FX.tilt(el));</script>
{% endblock %}
```

- [ ] **Step 5: Run the full suite to confirm no regression**

Run: `docker compose exec web python manage.py test game`
Expected: PASS — same test count and results as before this task (English output
is byte-identical to the pre-i18n templates).

- [ ] **Step 6: Commit**

```bash
git add game/templates/game/home.html game/templates/game/_card.html \
  game/templates/game/collection.html game/templates/game/card_detail.html
git commit -m "feat: translate card and collection templates"
```

---

### Task 5: Forms & social templates translated

**Files:**
- Modify: `game/templates/game/friends.html`
- Modify: `game/templates/game/propose.html`
- Modify: `game/templates/registration/login.html`
- Modify: `game/templates/registration/signup.html`

**Note:** `{{ form.as_div }}` on the login/signup pages renders Django's own
built-in `AuthenticationForm`/`UserCreationForm` labels, help text and error
messages — those already ship with Django's own compiled French translations, so
no code change is needed for them to appear in French once the language is `fr`.

**Files already asserting text this task touches:**
- `game/tests/test_friends.py:122-123` — `"Add a friend"`, `"No friends yet"`.
- `game/tests/test_friends.py:137` — `"No player named"` (this is backend text from
  `friends.py`, handled in Task 6 — unaffected by this task).
- `game/tests/test_friends.py:144` — `"Accept"`.
- `game/tests/test_proposals.py:54,67,106` — `id="card-editor"` (untouched),
  `"Club Échecs"` (posted data + widget placeholder — the placeholder itself is
  **not** translated, see Step 2 below), `maxlength="40"` (untouched, from
  `views.py`'s `ProposalForm.__init__`, not this task).

- [ ] **Step 1: Rewrite `game/templates/game/friends.html`**

```html
{% extends "base.html" %}{% load i18n %}
{% block title %}{% trans "Friends" %} · Pack Ema{% endblock %}
{% block content %}
<header class="page-head">
  <h1>{% trans "Friends" %}</h1>
</header>

<form class="panel add-friend" method="post">
  {% csrf_token %}
  <label for="username">{% trans "Add a friend" %}</label>
  <div class="add-friend-row">
    <input type="text" name="username" id="username" placeholder="{% trans 'Username' %}" required>
    <button class="btn">{% trans "Send request" %}</button>
  </div>
</form>

{% if incoming %}
<section class="panel friend-list">
  <h2>{% trans "Requests" %}</h2>
  <ul>
    {% for f in incoming %}
    <li>
      <strong>{{ f.from_user.username }}</strong>
      <span class="friend-actions">
        <form method="post" action="{% url 'friend_accept' f.pk %}">{% csrf_token %}<button class="btn small">{% trans "Accept" %}</button></form>
        <form method="post" action="{% url 'friend_remove' f.pk %}">{% csrf_token %}<button class="btn small ghost">{% trans "Decline" %}</button></form>
      </span>
    </li>
    {% endfor %}
  </ul>
</section>
{% endif %}

{% if outgoing %}
<section class="panel friend-list">
  <h2>{% trans "Sent requests" %}</h2>
  <ul>
    {% for f in outgoing %}
    <li>
      <strong>{{ f.to_user.username }}</strong>
      <span class="friend-actions">
        <span class="chip">{% trans "Pending" %}</span>
        <form method="post" action="{% url 'friend_remove' f.pk %}">{% csrf_token %}<button class="btn small ghost">{% trans "Cancel" %}</button></form>
      </span>
    </li>
    {% endfor %}
  </ul>
</section>
{% endif %}

<section class="panel friend-list">
  <h2>{% trans "Your friends" %}</h2>
  {% if friends %}
  <ul>
    {% for user in friends %}
    <li>
      <strong>{{ user.username }}</strong>
      <span class="friend-actions">
        <a class="btn small ghost" href="{% url 'friend_collection' user.username %}">{% trans "View collection" %}</a>
        <form method="post" action="{% url 'friend_remove' user.friendship_id %}">{% csrf_token %}<button class="btn small ghost">{% trans "Remove" %}</button></form>
      </span>
    </li>
    {% endfor %}
  </ul>
  {% else %}
  <p class="muted">{% trans "No friends yet — add one above." %}</p>
  {% endif %}
</section>
{% endblock %}
```

- [ ] **Step 2: Rewrite `game/templates/game/propose.html`**

```html
{% extends "base.html" %}{% load static i18n %}
{% block title %}{% trans "Propose a card" %} · Pack Ema{% endblock %}
{% block content %}
<header class="page-head">
  <div>
    <h1>{% trans "Propose a card" %}</h1>
    <p class="muted">{% trans "Design a card for a club, an association or a legend of the school. An admin reviews it and, if approved, it joins the packs." %}</p>
  </div>
</header>

<div class="editor">
  <form id="card-editor" class="panel editor-form" method="post" enctype="multipart/form-data" novalidate>
    {% csrf_token %}
    {% if at_limit %}<p class="flash flash-warning">{% blocktrans %}You already have {{ max_pending }} cards waiting for review. Wait for a decision before proposing another one.{% endblocktrans %}</p>{% endif %}
    {{ form.non_field_errors }}

    <fieldset>
      <legend>{% trans "Identity" %}</legend>
      <div class="field">
        <label for="{{ form.name.id_for_label }}">{% trans "Name" %} <span class="count" data-for="{{ form.name.id_for_label }}"></span></label>
        {{ form.name }}{{ form.name.errors }}
      </div>
      <div class="field">
        <label for="{{ form.tagline.id_for_label }}">{% trans "Tagline" %} <span class="count" data-for="{{ form.tagline.id_for_label }}"></span></label>
        {{ form.tagline }}{{ form.tagline.errors }}
      </div>
    </fieldset>

    <fieldset>
      <legend>{% trans "Images" %} <span class="muted">{% blocktrans %}PNG, JPG or WebP · max {{ max_mb }} MB{% endblocktrans %}</span></legend>
      <div class="uploads">
        <label class="upload" for="{{ form.logo.id_for_label }}">
          <span class="upload-title">{% trans "Logo" %}</span><span class="muted">{% trans "square works best" %}</span>
          <input type="file" name="logo" id="{{ form.logo.id_for_label }}" accept="image/*">
        </label>
        <label class="upload" for="{{ form.banner.id_for_label }}">
          <span class="upload-title">{% trans "Banner" %}</span><span class="muted">{% trans "wide picture, the card's art" %}</span>
          <input type="file" name="banner" id="{{ form.banner.id_for_label }}" accept="image/*">
        </label>
      </div>
      {{ form.logo.errors }}{{ form.banner.errors }}
    </fieldset>

    <fieldset>
      <legend>{% trans "Colors" %}</legend>
      <div class="colors">
        <label class="color">{{ form.primary_color }}<span>{% trans "Primary" %}</span></label>
        <label class="color">{{ form.secondary_color }}<span>{% trans "Secondary" %}</span></label>
      </div>
      {{ form.primary_color.errors }}{{ form.secondary_color.errors }}
    </fieldset>

    <fieldset>
      <legend>{% trans "Description" %}</legend>
      <div class="field">
        <label for="{{ form.description.id_for_label }}">{% trans "Shown on the card's page" %} <span class="count" data-for="{{ form.description.id_for_label }}"></span></label>
        {{ form.description }}{{ form.description.errors }}
      </div>
    </fieldset>

    <button class="btn block" {% if at_limit %}disabled{% endif %}>{% trans "Send for review" %}</button>
  </form>

  <aside class="editor-preview" id="preview" aria-label="{% trans 'Live preview' %}">
    {% include "game/_card.html" with card=preview %}
    <fieldset class="rarity-toggle">
      <legend>{% trans "Suggested rarity" %}</legend>
      {% for radio in form.rarity %}<label>{{ radio.tag }}<span>{{ radio.choice_label }}</span></label>{% endfor %}
    </fieldset>
    {{ form.rarity.errors }}
    <p class="muted small">{% trans "The admin can adjust it before approving the card." %}</p>
  </aside>
</div>

{% if proposals %}
<section class="panel my-proposals">
  <h2>{% trans "Your proposals" %}</h2>
  <ul>
    {% for p in proposals %}
    <li>
      <span class="swatch" style="--c1: {{ p.primary_color|default:'#3a3f58' }}; --c2: {{ p.secondary_color|default:'#1b1e2e' }}"></span>
      <strong>{{ p.name }}</strong>
      <span class="muted">{{ p.get_rarity_display }}</span>
      <span class="muted">{{ p.proposed_at|date:"j M Y" }}</span>
      <span class="chip status-{{ p.status }}">{{ p.get_status_display }}</span>
    </li>
    {% endfor %}
  </ul>
</section>
{% endif %}
{% endblock %}
{% block scripts %}
<script src="{% static 'game/fx.js' %}"></script>
<script src="{% static 'game/propose.js' %}"></script>
{% endblock %}
```

`form.rarity`'s radio labels and `p.get_rarity_display`/`p.get_status_display`
already translate automatically from Task 3 — nothing else to do here. The `name`
field's `"Club Échecs"` placeholder (defined in `views.py`, not this template) is
sample content, not UI chrome — it's deliberately left untranslated (see Task 6).

- [ ] **Step 3: Rewrite `game/templates/registration/login.html`**

```html
{% extends "base.html" %}{% load i18n %}
{% block title %}{% trans "Log in" %} · Pack Ema{% endblock %}
{% block content %}
<form method="post" class="panel form">{% csrf_token %}
  <span class="brand-mark big">EMA</span>
  <h1>{% trans "Welcome back" %}</h1>
  <p class="muted">{% trans "Your next pack might be waiting." %}</p>
  {{ form.as_div }}
  <button class="btn block">{% trans "Log in" %}</button>
  <p class="form-foot">{% trans "No account yet?" %} <a href="{% url 'signup' %}">{% trans "Sign up" %}</a></p>
</form>
{% endblock %}
```

- [ ] **Step 4: Rewrite `game/templates/registration/signup.html`**

```html
{% extends "base.html" %}{% load i18n %}
{% block title %}{% trans "Sign up" %} · Pack Ema{% endblock %}
{% block content %}
<form method="post" class="panel form">{% csrf_token %}
  <span class="brand-mark big">EMA</span>
  <h1>{% trans "Start your collection" %}</h1>
  <p class="muted">{% trans "Every club and student of the school, one pack at a time." %}</p>
  {{ form.as_div }}
  <button class="btn block">{% trans "Sign up" %}</button>
  <p class="form-foot">{% trans "Already playing?" %} <a href="{% url 'login' %}">{% trans "Log in" %}</a></p>
</form>
{% endblock %}
```

- [ ] **Step 5: Run the full suite to confirm no regression**

Run: `docker compose exec web python manage.py test game`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add game/templates/game/friends.html game/templates/game/propose.html \
  game/templates/registration/login.html game/templates/registration/signup.html
git commit -m "feat: translate friends, propose and auth templates"
```

---

### Task 6: Python-side user-facing strings translated

**Files:**
- Modify: `game/views.py`
- Modify: `game/friends.py`
- Modify: `game/packs.py`

**Interfaces:**
- Consumes: nothing new.
- Produces: no signature changes — `FriendError`/`PackUnavailable`/`ValidationError`
  still take a single message argument; callers doing `str(e)` (`views.py`'s
  `open_pack_view`, `friends`, `friend_accept`, `friend_remove`) keep working
  unchanged since `str()` on a `gettext_lazy` proxy resolves to the active
  language's text.

**Files already asserting this text:**
- `game/tests/test_friends.py:137` — `assertContains(response, "No player named")`
  (substring of the f-string this task replaces — must survive).
- `game/tests/test_proposals.py` — none assert on `ValidationError` message text,
  only on whether a `Card` was created/rejected.

**Watch out for:** `game/friends.py`'s `remove_friendship` currently names its
unused tuple element `_` (`deleted, _ = Friendship.objects.filter(...).delete()`).
Since this task imports `gettext_lazy as _` at module scope, that local `_` would
shadow the translation function for the rest of the function and break the
`FriendError(_("..."))` call right below it. Rename the local variable.

- [ ] **Step 1: Update `game/views.py`**

Add the import (top of file, with the other `django.*` imports):

```python
from django.utils.translation import gettext_lazy as _
```

Update `ProposalForm.Meta.widgets` (translate the two instructional placeholders;
leave `"Club Échecs"` — a sample club name, not an instruction — as is):

```python
    class Meta:
        model = Card
        fields = ["name", "tagline", "description", "logo", "banner", "primary_color", "secondary_color", "rarity"]
        widgets = {
            "name": forms.TextInput(attrs={"placeholder": "Club Échecs"}),
            "tagline": forms.TextInput(attrs={"placeholder": _("A short line under the name")}),
            "description": forms.Textarea(attrs={"rows": 5, "placeholder": _("Shown on the card's page: what is this club or person about?")}),
            "primary_color": forms.TextInput(attrs={"type": "color"}),
            "secondary_color": forms.TextInput(attrs={"type": "color"}),
        }
```

Update `_color` and `_image`:

```python
    def _color(self, field):
        value = self.cleaned_data[field]
        if value and not HEX_COLOR.fullmatch(value):  # ends up in an inline style attribute
            raise forms.ValidationError(_("Pick a color like #3a3f58."))
        return value

    def clean_rarity(self):
        return self.cleaned_data["rarity"] or Rarity.COMMON

    def clean_logo(self):
        return self._image("logo")

    def clean_banner(self):
        return self._image("banner")

    def _image(self, field):
        image = self.cleaned_data[field]
        if image and image.size > settings.PROPOSAL_MAX_IMAGE_BYTES:
            max_mb = settings.PROPOSAL_MAX_IMAGE_BYTES // (1024 * 1024)
            raise forms.ValidationError(_("Image too large (max %(max_mb)s MB).") % {"max_mb": max_mb})
        return image
```

Update the `friends` view:

```python
@login_required
def friends(request):
    if request.method == "POST":
        try:
            friend_logic.send_request(request.user, request.POST.get("username", ""))
            messages.success(request, _("Friend request sent!"))
        except friend_logic.FriendError as e:
            messages.error(request, str(e))
        return redirect("friends")
    return render(request, "game/friends.html", {
        "friends": friend_logic.friends_of(request.user),
        "incoming": Friendship.objects.filter(
            to_user=request.user, status=Friendship.Status.PENDING
        ).select_related("from_user"),
        "outgoing": Friendship.objects.filter(
            from_user=request.user, status=Friendship.Status.PENDING
        ).select_related("to_user"),
    })
```

Update `propose_card`'s success message:

```python
        messages.success(
            request,
            _("“%(name)s” was sent for review. If it's approved, it will show up in packs!") % {"name": card.name},
        )
```

- [ ] **Step 2: Update `game/friends.py`**

```python
from django.db.models import Q
from django.utils.translation import gettext_lazy as _

from .models import Friendship, User


class FriendError(Exception):
    """Can't complete this friend action (message is shown to the user)."""


def are_friends(a, b):
    return (
        Friendship.objects.filter(status=Friendship.Status.ACCEPTED)
        .filter(Q(from_user=a, to_user=b) | Q(from_user=b, to_user=a))
        .exists()
    )


def friends_of(user):
    """Accepted friends of `user`, as User instances (each tagged with its friendship_id)."""
    rows = (
        Friendship.objects.filter(status=Friendship.Status.ACCEPTED)
        .filter(Q(from_user=user) | Q(to_user=user))
        .select_related("from_user", "to_user")
    )
    friends = []
    for row in rows:
        other = row.to_user if row.from_user_id == user.id else row.from_user
        other.friendship_id = row.pk
        friends.append(other)
    return friends


def send_request(user, username):
    """Send a friend request from `user` to `username` (case-insensitive); auto-accepts a crossed request."""
    username = (username or "").strip()
    try:
        target = User.objects.get(username__iexact=username)
    except User.DoesNotExist:
        raise FriendError(_("No player named “%(username)s”.") % {"username": username})
    if target.pk == user.pk:
        raise FriendError(_("You can't add yourself."))
    if are_friends(user, target):
        raise FriendError(_("You're already friends with %(username)s.") % {"username": target.username})
    reverse_request = Friendship.objects.filter(
        from_user=target, to_user=user, status=Friendship.Status.PENDING
    ).first()
    if reverse_request:
        reverse_request.status = Friendship.Status.ACCEPTED
        reverse_request.save(update_fields=["status"])
        return reverse_request
    friendship, created = Friendship.objects.get_or_create(from_user=user, to_user=target)
    if not created:
        raise FriendError(_("You already sent %(username)s a request.") % {"username": target.username})
    return friendship


def accept_request(user, friendship_id):
    friendship = Friendship.objects.filter(
        pk=friendship_id, to_user=user, status=Friendship.Status.PENDING
    ).first()
    if not friendship:
        raise FriendError(_("That request no longer exists."))
    friendship.status = Friendship.Status.ACCEPTED
    friendship.save(update_fields=["status"])
    return friendship


def remove_friendship(user, friendship_id):
    """Decline a request, cancel one you sent, or remove an accepted friend — all just delete the row."""
    deleted, _counts = Friendship.objects.filter(pk=friendship_id).filter(Q(from_user=user) | Q(to_user=user)).delete()
    if not deleted:
        raise FriendError(_("That request no longer exists."))
```

(Note the `_counts` rename in `remove_friendship` — this is the fix for the
shadowing issue described above.)

- [ ] **Step 3: Update `game/packs.py`**

```python
import random
from django.conf import settings
from django.db import transaction
from django.db.models import Q
from django.utils import timezone
from django.utils.translation import gettext_lazy as _

from .models import Card, Pull, Rarity, Status, User

PACK_SIZE = 5
# Relative odds per slot. The last slot is the "hit": always Rare or better.
SLOT_ODDS = {Rarity.COMMON: 60, Rarity.UNCOMMON: 25, Rarity.RARE: 10, Rarity.EPIC: 4, Rarity.LEGENDARY: 1}
HIT_ODDS = {Rarity.RARE: 75, Rarity.EPIC: 20, Rarity.LEGENDARY: 5}


class PackUnavailable(Exception):
    """The user can't open a pack right now (message is shown to them)."""


def pick_rarity(odds, available, rng=random):
    """Weighted pick among the rarities that actually have cards."""
    choices = [r for r in odds if r in available] or [r for r in SLOT_ODDS if r in available]
    return rng.choices(choices, weights=[odds.get(r, SLOT_ODDS[r]) for r in choices])[0]


def open_pack(user, now=None):
    """Claim the user's pack and return its cards, rarest last."""
    now = now or timezone.now()
    with transaction.atomic():
        # A conditional UPDATE is the lock: two simultaneous requests can't both claim the pack.
        claimed = (
            User.objects.filter(pk=user.pk)
            .filter(Q(next_pack_at__isnull=True) | Q(next_pack_at__lte=now))
            .update(next_pack_at=now + settings.PACK_COOLDOWN)
        )
        if not claimed:
            raise PackUnavailable(_("Your next pack isn't ready yet."))
        pullable = Card.objects.filter(status=Status.APPROVED)
        available = set(pullable.values_list("rarity", flat=True).distinct())
        if not available:
            raise PackUnavailable(_("No cards exist yet. Run the import first."))  # rolls back the claim
        slots = [SLOT_ODDS] * (PACK_SIZE - 1) + [HIT_ODDS]
        # ponytail: order_by("?") scans the rarity bucket; fine for ~2k cards, precompute ids if it grows.
        cards = [
            pullable.filter(rarity=pick_rarity(odds, available)).order_by("?").first()
            for odds in slots
        ]
        Pull.objects.bulk_create(Pull(user=user, card=card, opened_at=now) for card in cards)
    user.next_pack_at = now + settings.PACK_COOLDOWN
    return sorted(cards, key=lambda card: card.rarity)
```

- [ ] **Step 4: Run the full suite to confirm no regression**

Run: `docker compose exec web python manage.py test game`
Expected: PASS — in particular `test_friends.py::FriendViewTests::test_send_request_error_is_flashed`
(checks `"No player named"` is still in the response) and every `PackUnavailable`/
`FriendError` `assertRaises` test (they don't check message text, only the
exception type, so they're unaffected either way).

- [ ] **Step 5: Commit**

```bash
git add game/views.py game/friends.py game/packs.py
git commit -m "feat: translate backend messages and form errors"
```

---

### Task 7: pack.js strings exposed via data attributes

**Files:**
- Modify: `game/templates/game/home.html` (add `data-msg-*` attributes to `#stage`)
- Modify: `game/static/game/pack.js`
- Modify: `game/tests/test_i18n.py`

**Interfaces:**
- Consumes: `game/templates/game/home.html`'s `#stage` element (already exists from
  Task 4).
- Produces: `stage.dataset.msgReady`, `.msgNextIn`, `.msgSessionExpired`,
  `.msgGenericError`, `.msgOpenFailed`, `.msgTapToReveal`, `.msgSwipeNext`,
  `.msgNewBadge` — read once at the top of the `pack.js` IIFE, consumed by the
  functions that used to hardcode these strings.

There's no JS test runner in this project (per `CLAUDE.md`, `game/static/game/*.js`
has no build/test tooling), so this task is verified by a Django template test
(the data attributes render with the right English text) plus a manual browser
check at the end of the step list.

- [ ] **Step 1: Write the failing test**

Add to `game/tests/test_i18n.py`:

```python
class PackDataAttributesTests(TestCase):
    def setUp(self):
        self.user = User.objects.create_user("ash", password="pw")
        self.client.force_login(self.user)

    def test_stage_exposes_translatable_pack_messages(self):
        response = self.client.get(reverse("home"))
        self.assertContains(response, 'data-msg-ready="A pack is ready!"')
        self.assertContains(response, 'data-msg-next-in="Next pack in {mm}:{ss}"')
        self.assertContains(response, 'data-msg-session-expired="Your session expired. Please log in again."')
        self.assertContains(response, 'data-msg-generic-error="Something went wrong. Try again."')
        self.assertContains(response, 'data-msg-open-failed="Could not open the pack. Try again."')
        self.assertContains(response, 'data-msg-tap-to-reveal="Tap to reveal"')
        self.assertContains(response, 'data-msg-swipe-next="Swipe it away, or tap for the next card"')
        self.assertContains(response, 'data-msg-new-badge="NEW!"')
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `docker compose exec web python manage.py test game.tests.test_i18n.PackDataAttributesTests -v 2`
Expected: FAIL — the attributes don't exist yet.

- [ ] **Step 3: Add the data attributes to `home.html`**

Replace the `#stage` opening tag:

```html
<section id="stage" class="stage"
  data-open-url="{% url 'open_pack' %}"
  data-next-pack-at="{{ next_pack_at|default:'' }}"
  data-msg-ready="{% trans 'A pack is ready!' %}"
  data-msg-next-in="{% trans 'Next pack in {mm}:{ss}' %}"
  data-msg-session-expired="{% trans 'Your session expired. Please log in again.' %}"
  data-msg-generic-error="{% trans 'Something went wrong. Try again.' %}"
  data-msg-open-failed="{% trans 'Could not open the pack. Try again.' %}"
  data-msg-tap-to-reveal="{% trans 'Tap to reveal' %}"
  data-msg-swipe-next="{% trans 'Swipe it away, or tap for the next card' %}"
  data-msg-new-badge="{% trans 'NEW!' %}">
```

- [ ] **Step 4: Run the test to verify it passes**

Run: `docker compose exec web python manage.py test game.tests.test_i18n.PackDataAttributesTests -v 2`
Expected: PASS.

- [ ] **Step 5: Make `pack.js` read the messages from `dataset`**

Right after the existing `const csrf = ...` line near the top of the IIFE, add:

```js
  const {
    msgReady, msgNextIn, msgSessionExpired, msgGenericError,
    msgOpenFailed, msgTapToReveal, msgSwipeNext, msgNewBadge,
  } = stage.dataset;
```

Then replace each hardcoded string with its `msg*` variable:

In `ready()`:
```js
    } else if (left <= 0) {
      text = msgReady;
    } else {
      const s = Math.ceil(left / 1000);
      text = msgNextIn.replace('{mm}', Math.floor(s / 60)).replace('{ss}', String(s % 60).padStart(2, '0'));
    }
```

In `fetchPack()`:
```js
        const data = await r.json().catch(() => ({
          error: r.redirected ? msgSessionExpired : msgGenericError,
        }));
```

In `fail()`:
```js
  function fail(err) {
    if (err && err.next_pack_at) nextPackAt = new Date(err.next_pack_at);
    message = (err && err.error) || msgOpenFailed;
    reset();
  }
```

In `revealAll()`:
```js
    for (const [i, c] of cards.entries()) {
      hintText.textContent = i === 0 ? msgTapToReveal : '';
      if (c.rarity >= 3) els[i].classList.add('hinting'); // Hearthstone-style: the back glows its rarity color
      await nextTap();
      await flip(els[i], c);
      hintText.textContent = i === 0 ? msgSwipeNext : '';
      const dir = await swipeOrTap(els[i]);
      await flyAway(els[i], i, dir);
    }
```

In `flip()`:
```js
    if (c.is_new) el.insertAdjacentHTML('beforeend', `<span class="new-badge">${msgNewBadge}</span>`);
```

In `showSummary()`:
```js
      item.innerHTML = c.html + (c.is_new ? `<span class="new-badge">${msgNewBadge}</span>` : '');
```

- [ ] **Step 6: Run the full suite to confirm no regression**

Run: `docker compose exec web python manage.py test game`
Expected: PASS.

- [ ] **Step 7: Manual browser check**

Run: `docker compose up` (or `PACK_COOLDOWN_SECONDS=10 docker compose up` for fast
retesting), open `http://localhost:8000`, log in, and tear open a pack. Confirm:
the countdown timer text, the "tap to reveal"/"swipe it away" hints, and the "NEW!"
badge all still render (proving `pack.js` is reading `dataset` correctly and no
`undefined` leaked into the UI), and the browser console has no errors.

- [ ] **Step 8: Commit**

```bash
git add game/templates/game/home.html game/static/game/pack.js game/tests/test_i18n.py
git commit -m "feat: translate pack.js UI strings via data attributes"
```

---

### Task 8: French translation catalog + end-to-end verification

**Files:**
- Create: `game/locale/fr/LC_MESSAGES/django.po`
- Create: `game/locale/fr/LC_MESSAGES/django.mo` (generated, binary — via `compilemessages`)
- Modify: `game/tests/test_i18n.py`

**Interfaces:**
- Consumes: every `{% trans %}`/`{% blocktrans %}`/`gettext_lazy` call site added
  in Tasks 2–7 — this task's `.po` file must contain a `msgid` for each one.

- [ ] **Step 1: Generate the `.po` scaffold**

Run: `docker compose exec web python manage.py makemessages -l fr`

This creates `game/locale/fr/LC_MESSAGES/django.po` (via `LOCALE_PATHS` from Task 1)
with every `msgid` this branch introduced, each with an empty `msgstr ""`.

- [ ] **Step 2: Fill in the French translations**

Open `game/locale/fr/LC_MESSAGES/django.po` and set each entry's `msgstr` using the
table below (match by `msgid` — the `#:` location comments `makemessages` generated
are just documentation and can be left as-is). Leave the header block (`Project-Id-Version`,
`Content-Type`, etc.) as `makemessages` generated it, but make sure it reads:

```
"Language: fr\n"
"Content-Type: text/plain; charset=UTF-8\n"
```

Translation table (`msgid` → `msgstr`):

| msgid | msgstr |
|---|---|
| `Packs` | `Packs` |
| `Collection` | `Collection` |
| `Friends` | `Amis` |
| `Propose` | `Proposer` |
| `Log out` | `Se déconnecter` |
| `Log out %(username)s` | `Se déconnecter (%(username)s)` |
| `Switch to English` | `Passer en anglais` |
| `Switch to French` | `Passer en français` |
| `Common` | `Commune` |
| `Uncommon` | `Peu commune` |
| `Rare` | `Rare` |
| `Epic` | `Épique` |
| `Legendary` | `Légendaire` |
| `Pending review` | `En attente de validation` |
| `Approved` | `Approuvée` |
| `Rejected` | `Refusée` |
| `Open a pack: drag across the top edge, or press Enter` | `Ouvrir un pack : glissez le long du bord supérieur, ou appuyez sur Entrée` |
| `Trading Card Game · IMT Alès` | `Jeu de cartes à collectionner · IMT Alès` |
| `Swipe across the top to tear it open` | `Glissez le long du haut pour l'ouvrir` |
| `Your pulls` | `Vos cartes obtenues` |
| `View collection` | `Voir la collection` |
| `Back` | `Retour` |
| `Skip` | `Passer` |
| `A pack is ready!` | `Un pack est prêt !` |
| `Next pack in {mm}:{ss}` | `Prochain pack dans {mm}:{ss}` |
| `Your session expired. Please log in again.` | `Votre session a expiré. Veuillez vous reconnecter.` |
| `Something went wrong. Try again.` | `Une erreur est survenue. Réessayez.` |
| `Could not open the pack. Try again.` | `Impossible d'ouvrir le pack. Réessayez.` |
| `Tap to reveal` | `Touchez pour révéler` |
| `Swipe it away, or tap for the next card` | `Glissez pour l'écarter, ou touchez pour la carte suivante` |
| `NEW!` | `NOUVEAU !` |
| `Subs` | `Abonnés` |
| `Posts` | `Publications` |
| `Events` | `Événements` |
| `%(username)s's collection` | `Collection de %(username)s` |
| `%(owned)s of %(total)s cards discovered` | `%(owned)s cartes découvertes sur %(total)s` |
| `/ %(total)s discovered` | `sur %(total)s` |
| `No cards yet` | `Aucune carte pour l'instant` |
| `%(username)s hasn't discovered any cards yet.` | `%(username)s n'a encore découvert aucune carte.` |
| `Tear open your first pack to start collecting.` | `Ouvrez votre premier pack pour commencer à collectionner.` |
| `Open a pack` | `Ouvrir un pack` |
| `Owned` | `Possédées` |
| `Subscribers` | `Abonnés` |
| `This page has no presentation yet.` | `Cette page n'a pas encore de présentation.` |
| `Add a friend` | `Ajouter un ami` |
| `Username` | `Nom d'utilisateur` |
| `Send request` | `Envoyer la demande` |
| `Requests` | `Demandes` |
| `Accept` | `Accepter` |
| `Decline` | `Refuser` |
| `Sent requests` | `Demandes envoyées` |
| `Pending` | `En attente` |
| `Cancel` | `Annuler` |
| `Your friends` | `Vos amis` |
| `Remove` | `Retirer` |
| `No friends yet — add one above.` | `Pas encore d'amis — ajoutez-en un ci-dessus.` |
| `Propose a card` | `Proposer une carte` |
| `Design a card for a club, an association or a legend of the school. An admin reviews it and, if approved, it joins the packs.` | `Concevez une carte pour un club, une association ou une légende de l'école. Un administrateur la valide puis, si elle est approuvée, elle rejoint les packs.` |
| `You already have %(max_pending)s cards waiting for review. Wait for a decision before proposing another one.` | `Vous avez déjà %(max_pending)s cartes en attente de validation. Attendez une décision avant d'en proposer une autre.` |
| `Identity` | `Identité` |
| `Name` | `Nom` |
| `Tagline` | `Accroche` |
| `Images` | `Images` |
| `PNG, JPG or WebP · max %(max_mb)s MB` | `PNG, JPG ou WebP · %(max_mb)s Mo max` |
| `Logo` | `Logo` |
| `square works best` | `un carré fonctionne mieux` |
| `Banner` | `Bannière` |
| `wide picture, the card's art` | `image large, l'illustration de la carte` |
| `Colors` | `Couleurs` |
| `Primary` | `Primaire` |
| `Secondary` | `Secondaire` |
| `Description` | `Description` |
| `Shown on the card's page` | `Affiché sur la page de la carte` |
| `Send for review` | `Envoyer pour validation` |
| `Live preview` | `Aperçu en direct` |
| `Suggested rarity` | `Rareté suggérée` |
| `The admin can adjust it before approving the card.` | `L'administrateur peut l'ajuster avant d'approuver la carte.` |
| `Your proposals` | `Vos propositions` |
| `Log in` | `Connexion` |
| `Welcome back` | `Content de vous revoir` |
| `Your next pack might be waiting.` | `Votre prochain pack vous attend peut-être.` |
| `No account yet?` | `Pas encore de compte ?` |
| `Sign up` | `S'inscrire` |
| `Start your collection` | `Commencez votre collection` |
| `Every club and student of the school, one pack at a time.` | `Chaque club et étudiant de l'école, un pack à la fois.` |
| `Already playing?` | `Déjà inscrit ?` |
| `A short line under the name` | `Une courte ligne sous le nom` |
| `Shown on the card's page: what is this club or person about?` | `Affiché sur la page de la carte : de quoi parle ce club ou cette personne ?` |
| `Pick a color like #3a3f58.` | `Choisissez une couleur comme #3a3f58.` |
| `Image too large (max %(max_mb)s MB).` | `Image trop grande (max %(max_mb)s Mo).` |
| `Friend request sent!` | `Demande d'ami envoyée !` |
| `“%(name)s” was sent for review. If it's approved, it will show up in packs!` | `« %(name)s » a été envoyée pour validation. Si elle est approuvée, elle apparaîtra dans les packs !` |
| `No player named “%(username)s”.` | `Aucun joueur nommé « %(username)s ».` |
| `You can't add yourself.` | `Vous ne pouvez pas vous ajouter vous-même.` |
| `You're already friends with %(username)s.` | `Vous êtes déjà ami avec %(username)s.` |
| `You already sent %(username)s a request.` | `Vous avez déjà envoyé une demande à %(username)s.` |
| `That request no longer exists.` | `Cette demande n'existe plus.` |
| `Your next pack isn't ready yet.` | `Votre prochain pack n'est pas encore prêt.` |
| `No cards exist yet. Run the import first.` | `Aucune carte n'existe encore. Lancez d'abord l'import.` |

If `makemessages` produced any `msgid` not in this table (it shouldn't — this table
is exhaustive for everything added in Tasks 2–7), stop and check for a typo between
a template/Python string and this plan before guessing a translation.

- [ ] **Step 3: Compile the catalog**

Run: `docker compose exec web python manage.py compilemessages`
Expected: `processing file django.po in .../game/locale/fr/LC_MESSAGES` with no
errors, producing `game/locale/fr/LC_MESSAGES/django.mo`.

- [ ] **Step 4: Write the end-to-end French-rendering test**

Add to `game/tests/test_i18n.py` (new import at top: `from django.utils import translation`,
and `from game.models import Rarity`):

```python
class FrenchRenderingTests(TestCase):
    def setUp(self):
        self.user = User.objects.create_user("ash", password="pw")
        self.client.force_login(self.user)

    def test_home_renders_in_french_via_accept_language(self):
        response = self.client.get(reverse("home"), HTTP_ACCEPT_LANGUAGE="fr")
        self.assertContains(response, "Un pack est prêt !")
        self.assertContains(response, "Vos cartes obtenues")

    def test_switching_language_via_the_switcher_persists_across_requests(self):
        self.client.post(reverse("set_language"), {"language": "fr", "next": "/"})
        response = self.client.get(reverse("friends"))
        self.assertContains(response, "Amis")
        self.assertContains(response, "Ajouter un ami")

    def test_rarity_label_is_translated(self):
        with translation.override("fr"):
            self.assertEqual(str(Rarity.LEGENDARY.label), "Légendaire")

    def test_english_is_unaffected_by_the_french_catalog(self):
        response = self.client.get(reverse("home"))
        self.assertContains(response, "A pack is ready!")
```

- [ ] **Step 5: Run the new tests to verify they pass**

Run: `docker compose exec web python manage.py test game.tests.test_i18n -v 2`
Expected: PASS (all tests in the file, including the 4 new ones).

- [ ] **Step 6: Run the full suite one last time**

Run: `docker compose exec web python manage.py test game`
Expected: PASS — every test in the project, unchanged count from before this
branch, confirming English output is untouched and nothing else regressed.

- [ ] **Step 7: Commit**

```bash
git add game/locale/fr/LC_MESSAGES/django.po game/locale/fr/LC_MESSAGES/django.mo game/tests/test_i18n.py
git commit -m "feat: add French translation catalog and end-to-end i18n tests"
```

- [ ] **Step 8: Final manual pass**

Run: `docker compose up`, open `http://localhost:8000`, click the FR button in the
header, and click through every page (packs, collection, a card detail, friends,
propose, log out, log in, sign up) confirming the UI reads naturally in French and
nothing shows a raw `msgid` or an empty string. Switch back to EN and confirm
nothing broke.
